#ifndef __DEFAULT_TEMPLATE_H__
#define __DEFAULT_TEMPLATE_H__

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * @brief 在运行目录下创建一个名为project_name的默认模板项目
 * 
 * @param project_name 
 */
void createDefaultTemplateProject(char* project_name, char* project_type);

#endif